<?php


/**
 * Replace all occurrences of the array keys by the corresponding values.
 *
 * @require PHP >= 5.4
 * @param string $subject PHP serialized data
 * @param array $replace_pairs The replace_pairs parameter may be used as a substitute for to and from in which case it's an array in the form array('from' => 'to', ...).
 * @return string This function returns a PHP serialized string with the replaced values.
 */
function strtr_serialized($subject, array $replace_pairs)
{
    // @todo write your code here

    return $subject;
}

